# 2023-04-12


* 자료형

* 변수
```
        정수
        int(-21억~21억), float(인트보다 큰 값을 입력할 때)
        실수
        float(적은 자릿수), double(많은 자릿수, 정밀)
        문자열
        char(하나의 문자를 표현하기 위한), String(여러 문자를 표현하기위한)
        참과 거짓
        boolean : true, false 참과 거짓을 표현하는
```
